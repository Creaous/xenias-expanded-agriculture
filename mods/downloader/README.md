# Prism Launcher Index Downloader

A basic Python script that reads .toml files created by Prism Launcher in the .index folder and downloads them.

## Setup

1. `python -m venv venv`
2. `source venv/bin/activate`
3. `python -m pip install -r requirements.txt`
